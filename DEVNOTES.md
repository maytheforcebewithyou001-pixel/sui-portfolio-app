# FORCE CAPITAL - 開発メモ

## 2026-05-04

### セッションTTL集約 (`5b14cf6`)
- `SESSION_TTL_SEC` を `config.py` に集約、値を8時間に変更
- app.py から定数のハードコードを除去

### 市場・口座の拡充 (`34ab041`)
- 市場に「暗号資産」「債券/国債」「コモディティ」の3種を追加
- 口座に野村證券・大和証券・SMBC日興証券・PayPay証券など7種を追加
- `ACCT_BADGE_MAP` / `style.py` にバッジ色を追加

---

## 2026-05-05

### 資産額ランクバッジ (`7075c31`, `2b09720`)
- ヘッダーの評価額横にターミナル風ランクバッジを表示
- 初版は6段階 → 21段階に細分化
  - 100万〜1000万: 100万単位 (CADET → COMMANDER)
  - 1000万〜3000万: 500万単位 (STRATEGIST → GENERAL)
  - 3000万〜1億: 1000万単位 (ADMIRAL → LEGEND)
- バーゲージは固定10セグメント（達成率ベース表示）
- 最高ランク到達時は脈動アニメーション付き
- 変更ファイル: `config.py` (RANK_TIERS, get_rank), `style.py`, `app.py`

### ランク達成一覧タブ (`aed8ea6`)
- 「🏆 ランク」タブを新設 (`tabs/tab_rank.py`)
- 現在ランクの大型表示（42px + テキストグロー + 次ランクまでの残額）
- 全21ランクの達成状況リスト（達成済み/現在/未達成を色分け）

### 投資信託の前日比 (`b4d0c9c`)
- 投資信託の前日比がNoneだった問題を修正
- 「💾 記録」ボタン押下時に投信個別価格を `FundHistory` シートに保存
- 次回ロード時に前回記録価格との差分から `dod_pct` を算出
- 変更ファイル: `data.py` (save_fund_history, load_prev_fund_prices), `calc.py`, `app.py`
- 新規シート: `FundHistory` (日付 / 銘柄コード / 基準価額)

### 2FA一時無効化 (`4623fc1`, `f0e53b6`)
- ログイン画面の6桁TOTP入力欄を非表示に
- TOTP検証ロジックもスキップ（入力欄だけ消した初回修正ではログイン不可になった）
- 一般公開時に再有効化予定。`_verify_totp()` 関数は維持
