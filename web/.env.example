# Next.js(web/)の環境変数テンプレート。実値は .env.local (gitignore済) か Vercel の設定へ。
# ローカル: 未設定なら http://localhost:8000 が使われる
# 本番   : Cloud Run のURL (例 https://fc-api-xxxxx.a.run.app)
NEXT_PUBLIC_API_BASE=http://localhost:8000
