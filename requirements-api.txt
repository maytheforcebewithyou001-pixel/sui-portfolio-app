# FastAPI版の依存。共有ロジック(data/calc/market/jquants等)の依存は requirements.txt を引き継ぐ
# (2026-08-30 Streamlit退役済み — streamlit系はrequirements.txtから削除済)
-r requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27  # テスト(TestClient)用
